using System;

namespace calculator
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("I'm a simple claculator that can solve a problem involving two numbers and an operation.");
            Console.WriteLine("Put the first number you'd like for the problem.");

            int firstnum = Console.ReadLine();

            Console.WriteLine("Now put in the operation you'd like to use. Write add, minus, times, or divide, and write it in lowercase.");

            String operation = Console.ReadLine();

            Console.WriteLine("Now Put the second number.");

            int secondnum = Console.ReadLine();

            if (operation = add)
            {
                int answer = firstnum + secondnum 
            }

            if (operation = minus)
            {
                int answer = firstnum - secondnum 
            }

            if (operation = times)
            {
                int answer = firstnum * secondnum 
            }

            if (operation = divide)
            {
                int answer = firstnum / secondnum 
            }

            Console.WriteLine("The answer to your problem is " + answer);

        }
    }
}
